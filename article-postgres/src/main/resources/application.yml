spring:
  application:
    name: article-postgres
  profiles:
    active: dev
  cloud:
    config:
      uri: http://configserver:8071

logging:
  level:
    org.springframework.web: WARN
    com.example: DEBUG
